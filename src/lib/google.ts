/**
 * @deprecated Use `@/shared/api/google` instead
 * 이 파일은 하위 호환성을 위해 유지됩니다.
 */
export {
  getGoogleImageResults,
  type ProcessedImageResult,
  type ImageSearchResponse,
} from '@/shared/api/google';
